package eapli.elearning.postitmanagement.persistence;

import eapli.elearning.postitmanagement.domain.PostIt;
import eapli.elearning.sharedboardmanagement.dto.SharedBoardDTO;
import eapli.framework.domain.repositories.DomainRepository;

import java.util.List;

/**
 * @author : Ricardo Venâncio - 1210828
 **/
//public interface PostItRepository extends DomainRepository<Integer, PostIt> {
//    /**
//     * Checks if there is an existing post-it in the cell specified by in row and column.
//     * @param sharedBoardTitle shared board to check.
//     * @param row row to check.
//     * @param column column to check.
//     * @return true if there is already a post-it, otherwise return false if there isn't.
//     */
//    boolean checkPostItCell(String sharedBoardTitle, Integer row, Integer column);
//    List<PostIt> listPostItsOfBoard(SharedBoardDTO board);
//
//    PostIt findPostItById(int PostItId);
//
//    PostIt changePostIt(int row,int column,String content,PostIt postIt);
//}
